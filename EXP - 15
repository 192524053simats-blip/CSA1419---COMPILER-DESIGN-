#include <stdio.h>
#include <ctype.h>

int main()
{
    char ch;
    int characters = 0;
    int words = 0;
    int lines = 0;
    int inWord = 0;

    printf("Enter text:\n");

    while ((ch = getchar()) != EOF)
    {
        characters++;

        if (ch == '\n')
            lines++;

        if (isspace(ch))
        {
            inWord = 0;
        }
        else if (!inWord)
        {
            words++;
            inWord = 1;
        }
    }

    printf("\nCharacters = %d\n", characters);
    printf("Words = %d\n", words);
    printf("Lines = %d\n", lines);

    return 0;
}
